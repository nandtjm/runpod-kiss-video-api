# RTX 5090 Specific Requirements
# Optimized for sm_120 CUDA capability

# RunPod serverless (latest)
runpod>=1.7.0,<2.0.0

# PyTorch 2.7+ with RTX 5090 sm_120 support
# (Installed separately in Dockerfile with specific index)
# torch>=2.7.0 (with sm_120 support)
# torchvision>=0.22.0  
# torchaudio>=2.7.0

# Hugging Face ecosystem (RTX 5090 compatible versions)
huggingface_hub>=0.25.0
transformers>=4.45.0
accelerate>=1.0.0
diffusers>=0.31.0

# Image/Video processing
opencv-python-headless>=4.10.0,<5.0.0
Pillow>=10.4.0,<12.0.0
numpy>=1.26.0,<3.0.0

# Video processing optimized for RTX 5090
imageio>=2.35.0
imageio-ffmpeg>=0.5.1

# Core utilities
requests>=2.32.0,<3.0.0
tqdm>=4.66.0

# GPU monitoring and optimization for RTX 5090
psutil>=6.0.0
gputil>=1.4.0
pynvml>=11.5.0

# Performance optimization for RTX 5090
ninja>=1.11.0
safetensors>=0.4.0

# Memory optimization (important for RTX 5090's 32GB VRAM)
memory-profiler>=0.61.0

# Build tools (needed if compiling PyTorch from source for RTX 5090)
cmake>=3.26.0
pyyaml>=6.0